---
title: 组件全景
description: Callout、Tabs、Cards、Steps、FileTree、Mermaid、Math、Collapse 等示例。
searchable: true
---

# 组件全景

## Callout

<Callout type="warning" emoji="⚠️">
  在生产环境请为每次调用配置 trace id。
</Callout>

## Tabs

<Tabs items={["cargo", "bun", "pnpm"]}>
  <Tabs.Tab>

```bash
cargo test
```

  </Tabs.Tab>
  <Tabs.Tab>

```bash
bun test
```

  </Tabs.Tab>
  <Tabs.Tab>

```bash
pnpm test
```

  </Tabs.Tab>
</Tabs>

## Cards

<Cards>
  <Cards.Card title="Client" href="/zh/docs/clients" arrow />
  <Cards.Card title="Agent" href="/zh/docs/agent" arrow />
  <Cards.Card title="Tools" href="/zh/docs/tools" arrow />
</Cards>

## Steps

<Steps>
### 初始化 Client

准备 provider、base_url 和 api_key。

### 注册工具

通过类型化参数定义 execute handler。

### 调用 Agent

使用 `run()` 并根据 `AiError` 恢复。
</Steps>

## FileTree

<FileTree>
  <FileTree.Folder name="src" defaultOpen>
    <FileTree.File name="lib.rs" />
    <FileTree.Folder name="agent" defaultOpen>
      <FileTree.File name="mod.rs" />
      <FileTree.File name="runner.rs" />
    </FileTree.Folder>
    <FileTree.Folder name="model_adapters">
      <FileTree.File name="openai.rs" />
      <FileTree.File name="anthropic.rs" />
    </FileTree.Folder>
  </FileTree.Folder>
</FileTree>

## Mermaid

<Mermaid
  chart={`sequenceDiagram
  participant U as User
  participant A as Agent
  participant T as Tool
  U->>A: run(prompt)
  A->>T: execute(args)
  T-->>A: result
  A-->>U: final answer`}
/>

## Math

拉格朗日形式：

$$L(\theta)=\sum_{i=1}^{n}\log p_\theta(y_i \mid x_i)$$

## Collapse

<Collapse isOpen={true}>
  <div style={{ padding: "0.8rem", border: "1px solid #d5ddeb", borderRadius: "8px" }}>
    默认展开，可用于折叠高级参数说明。
  </div>
</Collapse>
